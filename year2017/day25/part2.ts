import { Config } from '../..';

export async function run(data: string[], config: Config): Promise<string | number> {
  return 'nothing to see here';
}

export const testResult = 'nothing to see here';
